name: CRM cached-identity validators (live sandbox drift)

# Daily drift check for two opt-in integration suites against the real
# HubSpot, Salesforce, Pipedrive, and Zoho sandboxes:
#
#   1. `validateCachedIdentity.integration.test.ts` — the
#      `CrmCallerIdentityRevalidationScheduler`'s parsers; catches
#      upstream "missing record" shape changes that would silently
#      disable proactive cleanup in production.
#   2. `appointmentBooked.integration.test.ts` — each first-party CRM
#      adapter's `appointment.booked` `execute()` path; catches upstream
#      auto-promote shape changes that would silently break pipeline
#      reporting on the booking handoff. Mirrors the validator suite's
#      per-provider gating so each provider can ship independently.
#
# The two suites run as separate vitest invocations so a failure in one
# can be triaged without re-running the other, and so the run summary
# distinguishes drift in the read path from drift in the write path.
#
# Provisioning, fixture-record creation, secret naming, and rotation are
# documented in docs/runbooks/crm-sandbox-credentials.md.

on:
  push:
    branches: [main, master]
    paths:
      - "platform/integrations/connectors/adapters/hubspot.ts"
      - "platform/integrations/connectors/adapters/salesforce.ts"
      - "platform/integrations/connectors/adapters/pipedrive.ts"
      - "platform/integrations/connectors/adapters/zoho.ts"
      - "platform/integrations/connectors/adapters/validateCachedIdentity*.ts"
      - "platform/integrations/connectors/adapters/appointmentBooked.integration.test.ts"
      - "platform/integrations/connectors/types.ts"
      - ".github/workflows/crm-cached-identity-validators.yml"
  workflow_dispatch:

permissions:
  contents: read
  # Needed so the alert step can open / update / close the tracking
  # issue used as the on-call notification surface for this job.
  issues: write

concurrency:
  group: crm-cached-identity-validators
  cancel-in-progress: true

jobs:
  validate:
    name: Validate cached-identity validators against live CRM sandboxes
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      # HubSpot — private-app token, never auto-expires.
      HUBSPOT_SANDBOX_ACCESS_TOKEN: ${{ secrets.HUBSPOT_SANDBOX_ACCESS_TOKEN }}
      HUBSPOT_SANDBOX_DELETED_CONTACT_ID: ${{ secrets.HUBSPOT_SANDBOX_DELETED_CONTACT_ID }}
      HUBSPOT_SANDBOX_DELETED_COMPANY_ID: ${{ secrets.HUBSPOT_SANDBOX_DELETED_COMPANY_ID }}
      HUBSPOT_SANDBOX_DELETED_DEAL_ID: ${{ secrets.HUBSPOT_SANDBOX_DELETED_DEAL_ID }}
      # Optional: when set, the appointment.booked suite asserts the
      # auto-promoted Deal lands in this pipeline/stage. When unset, it
      # only asserts the Deal was created.
      HUBSPOT_SANDBOX_APPOINTMENT_PIPELINE_ID: ${{ secrets.HUBSPOT_SANDBOX_APPOINTMENT_PIPELINE_ID }}
      HUBSPOT_SANDBOX_APPOINTMENT_STAGE_ID: ${{ secrets.HUBSPOT_SANDBOX_APPOINTMENT_STAGE_ID }}

      # Salesforce — short-lived access token, minted per run from these
      # credentials so the daily cron can't outlive the session policy. The
      # mint step writes `SALESFORCE_SANDBOX_ACCESS_TOKEN` and
      # `SALESFORCE_SANDBOX_INSTANCE_URL` into $GITHUB_ENV for vitest to read.
      SALESFORCE_SANDBOX_CLIENT_ID: ${{ secrets.SALESFORCE_SANDBOX_CLIENT_ID }}
      SALESFORCE_SANDBOX_CLIENT_SECRET: ${{ secrets.SALESFORCE_SANDBOX_CLIENT_SECRET }}
      SALESFORCE_SANDBOX_USERNAME: ${{ secrets.SALESFORCE_SANDBOX_USERNAME }}
      SALESFORCE_SANDBOX_PASSWORD: ${{ secrets.SALESFORCE_SANDBOX_PASSWORD }}
      SALESFORCE_SANDBOX_SECURITY_TOKEN: ${{ secrets.SALESFORCE_SANDBOX_SECURITY_TOKEN }}
      SALESFORCE_SANDBOX_LOGIN_URL: ${{ secrets.SALESFORCE_SANDBOX_LOGIN_URL }}
      SALESFORCE_SANDBOX_DELETED_CONTACT_ID: ${{ secrets.SALESFORCE_SANDBOX_DELETED_CONTACT_ID }}
      SALESFORCE_SANDBOX_DELETED_ACCOUNT_ID: ${{ secrets.SALESFORCE_SANDBOX_DELETED_ACCOUNT_ID }}
      SALESFORCE_SANDBOX_DELETED_OPPORTUNITY_ID: ${{ secrets.SALESFORCE_SANDBOX_DELETED_OPPORTUNITY_ID }}

      # Pipedrive — personal API token (or OAuth access token); does not
      # auto-expire.
      PIPEDRIVE_SANDBOX_ACCESS_TOKEN: ${{ secrets.PIPEDRIVE_SANDBOX_ACCESS_TOKEN }}
      PIPEDRIVE_SANDBOX_API_TOKEN: ${{ secrets.PIPEDRIVE_SANDBOX_API_TOKEN }}
      PIPEDRIVE_SANDBOX_COMPANY_DOMAIN: ${{ secrets.PIPEDRIVE_SANDBOX_COMPANY_DOMAIN }}
      PIPEDRIVE_SANDBOX_DELETED_PERSON_ID: ${{ secrets.PIPEDRIVE_SANDBOX_DELETED_PERSON_ID }}
      PIPEDRIVE_SANDBOX_DELETED_ORG_ID: ${{ secrets.PIPEDRIVE_SANDBOX_DELETED_ORG_ID }}
      PIPEDRIVE_SANDBOX_DELETED_DEAL_ID: ${{ secrets.PIPEDRIVE_SANDBOX_DELETED_DEAL_ID }}
      # Optional: when set, the appointment.booked suite asserts the
      # auto-promoted Deal lands in this pipeline/stage.
      PIPEDRIVE_SANDBOX_APPOINTMENT_PIPELINE_ID: ${{ secrets.PIPEDRIVE_SANDBOX_APPOINTMENT_PIPELINE_ID }}
      PIPEDRIVE_SANDBOX_APPOINTMENT_STAGE_ID: ${{ secrets.PIPEDRIVE_SANDBOX_APPOINTMENT_STAGE_ID }}

      # Zoho — access tokens expire after ~1 hour, so we store the long-lived
      # refresh token + OAuth app credentials and mint per run. The mint step
      # writes `ZOHO_SANDBOX_ACCESS_TOKEN` and `ZOHO_SANDBOX_API_DOMAIN` into
      # $GITHUB_ENV.
      ZOHO_SANDBOX_REFRESH_TOKEN: ${{ secrets.ZOHO_SANDBOX_REFRESH_TOKEN }}
      ZOHO_SANDBOX_CLIENT_ID: ${{ secrets.ZOHO_SANDBOX_CLIENT_ID }}
      ZOHO_SANDBOX_CLIENT_SECRET: ${{ secrets.ZOHO_SANDBOX_CLIENT_SECRET }}
      ZOHO_SANDBOX_ACCOUNTS_URL: ${{ secrets.ZOHO_SANDBOX_ACCOUNTS_URL }}
      ZOHO_SANDBOX_DELETED_CONTACT_ID: ${{ secrets.ZOHO_SANDBOX_DELETED_CONTACT_ID }}
      ZOHO_SANDBOX_DELETED_ACCOUNT_ID: ${{ secrets.ZOHO_SANDBOX_DELETED_ACCOUNT_ID }}
      ZOHO_SANDBOX_DELETED_DEAL_ID: ${{ secrets.ZOHO_SANDBOX_DELETED_DEAL_ID }}
      # Optional: when set, the appointment.booked suite asserts the
      # auto-promoted Deal lands in this Layout (`appointment_pipeline_id`)
      # and Stage name (`appointment_stage_id`).
      ZOHO_SANDBOX_APPOINTMENT_PIPELINE_ID: ${{ secrets.ZOHO_SANDBOX_APPOINTMENT_PIPELINE_ID }}
      ZOHO_SANDBOX_APPOINTMENT_STAGE_ID: ${{ secrets.ZOHO_SANDBOX_APPOINTMENT_STAGE_ID }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: bash scripts/ci-npm-ci.sh

      # ─── Per-run token minting for short-lived providers ──────────────────
      # Salesforce sessions expire on the connected app's policy; Zoho access
      # tokens expire in ~1h. Mint a fresh one at the top of each run from
      # long-lived credentials so the daily cron can't outlive the session.

      - name: Mint Salesforce sandbox access token
        if: env.SALESFORCE_SANDBOX_CLIENT_ID != '' && env.SALESFORCE_SANDBOX_CLIENT_SECRET != '' && env.SALESFORCE_SANDBOX_USERNAME != '' && env.SALESFORCE_SANDBOX_PASSWORD != ''
        run: |
          login_url="${SALESFORCE_SANDBOX_LOGIN_URL:-https://test.salesforce.com}"
          # Salesforce password-flow concatenates password + security token.
          password_with_token="${SALESFORCE_SANDBOX_PASSWORD}${SALESFORCE_SANDBOX_SECURITY_TOKEN}"
          response="$(curl -sS -X POST "${login_url}/services/oauth2/token" \
            --data-urlencode "grant_type=password" \
            --data-urlencode "client_id=${SALESFORCE_SANDBOX_CLIENT_ID}" \
            --data-urlencode "client_secret=${SALESFORCE_SANDBOX_CLIENT_SECRET}" \
            --data-urlencode "username=${SALESFORCE_SANDBOX_USERNAME}" \
            --data-urlencode "password=${password_with_token}")"
          access_token="$(node -e 'const j=JSON.parse(process.argv[1]); if(!j.access_token){console.error("no access_token in response:", JSON.stringify(j)); process.exit(1)} process.stdout.write(j.access_token)' "$response")"
          instance_url="$(node -e 'const j=JSON.parse(process.argv[1]); process.stdout.write(j.instance_url || "")' "$response")"
          echo "::add-mask::$access_token"
          {
            echo "SALESFORCE_SANDBOX_ACCESS_TOKEN=${access_token}"
            echo "SALESFORCE_SANDBOX_INSTANCE_URL=${instance_url}"
          } >> "$GITHUB_ENV"
          echo "Salesforce sandbox token minted (instance_url=${instance_url})."

      - name: Mint Zoho sandbox access token
        if: env.ZOHO_SANDBOX_REFRESH_TOKEN != '' && env.ZOHO_SANDBOX_CLIENT_ID != '' && env.ZOHO_SANDBOX_CLIENT_SECRET != ''
        run: |
          accounts_url="${ZOHO_SANDBOX_ACCOUNTS_URL:-https://accounts.zoho.com}"
          response="$(curl -sS -X POST "${accounts_url}/oauth/v2/token" \
            --data-urlencode "grant_type=refresh_token" \
            --data-urlencode "client_id=${ZOHO_SANDBOX_CLIENT_ID}" \
            --data-urlencode "client_secret=${ZOHO_SANDBOX_CLIENT_SECRET}" \
            --data-urlencode "refresh_token=${ZOHO_SANDBOX_REFRESH_TOKEN}")"
          access_token="$(node -e 'const j=JSON.parse(process.argv[1]); if(!j.access_token){console.error("no access_token in response:", JSON.stringify(j)); process.exit(1)} process.stdout.write(j.access_token)' "$response")"
          api_domain="$(node -e 'const j=JSON.parse(process.argv[1]); process.stdout.write(j.api_domain || "https://www.zohoapis.com")' "$response")"
          echo "::add-mask::$access_token"
          {
            echo "ZOHO_SANDBOX_ACCESS_TOKEN=${access_token}"
            echo "ZOHO_SANDBOX_API_DOMAIN=${api_domain}"
          } >> "$GITHUB_ENV"
          echo "Zoho sandbox token minted (api_domain=${api_domain})."

      # ─── Pre-flight summary ───────────────────────────────────────────────
      - name: Report which provider blocks will run
        run: |
          set -u
          mkdir -p .ci-out
          {
            echo "## CRM cached-identity validator providers"
            echo ""
            echo "| Provider   | Token | Required extras   | At least one deleted ID | Will run? |"
            echo "| ---------- | ----- | ----------------- | ----------------------- | --------- |"

            tok=$([ -n "${HUBSPOT_SANDBOX_ACCESS_TOKEN:-}" ] && echo yes || echo no)
            ids=$( { [ -n "${HUBSPOT_SANDBOX_DELETED_CONTACT_ID:-}" ] || [ -n "${HUBSPOT_SANDBOX_DELETED_COMPANY_ID:-}" ] || [ -n "${HUBSPOT_SANDBOX_DELETED_DEAL_ID:-}" ]; } && echo yes || echo no)
            run=$( { [ "$tok" = "yes" ] && [ "$ids" = "yes" ]; } && echo yes || echo no)
            echo "| HubSpot    | $tok   | n/a               | $ids                     | $run       |"

            tok=$([ -n "${SALESFORCE_SANDBOX_ACCESS_TOKEN:-}" ] && echo yes || echo no)
            inst=$([ -n "${SALESFORCE_SANDBOX_INSTANCE_URL:-}" ] && echo yes || echo no)
            ids=$( { [ -n "${SALESFORCE_SANDBOX_DELETED_CONTACT_ID:-}" ] || [ -n "${SALESFORCE_SANDBOX_DELETED_ACCOUNT_ID:-}" ] || [ -n "${SALESFORCE_SANDBOX_DELETED_OPPORTUNITY_ID:-}" ]; } && echo yes || echo no)
            run=$( { [ "$tok" = "yes" ] && [ "$inst" = "yes" ] && [ "$ids" = "yes" ]; } && echo yes || echo no)
            echo "| Salesforce | $tok   | instance_url=$inst | $ids                     | $run       |"

            tok=$( { [ -n "${PIPEDRIVE_SANDBOX_ACCESS_TOKEN:-}" ] || [ -n "${PIPEDRIVE_SANDBOX_API_TOKEN:-}" ]; } && echo yes || echo no)
            ids=$( { [ -n "${PIPEDRIVE_SANDBOX_DELETED_PERSON_ID:-}" ] || [ -n "${PIPEDRIVE_SANDBOX_DELETED_ORG_ID:-}" ] || [ -n "${PIPEDRIVE_SANDBOX_DELETED_DEAL_ID:-}" ]; } && echo yes || echo no)
            run=$( { [ "$tok" = "yes" ] && [ "$ids" = "yes" ]; } && echo yes || echo no)
            echo "| Pipedrive  | $tok   | n/a               | $ids                     | $run       |"

            tok=$([ -n "${ZOHO_SANDBOX_ACCESS_TOKEN:-}" ] && echo yes || echo no)
            dom=$([ -n "${ZOHO_SANDBOX_API_DOMAIN:-}" ] && echo yes || echo no)
            ids=$( { [ -n "${ZOHO_SANDBOX_DELETED_CONTACT_ID:-}" ] || [ -n "${ZOHO_SANDBOX_DELETED_ACCOUNT_ID:-}" ] || [ -n "${ZOHO_SANDBOX_DELETED_DEAL_ID:-}" ]; } && echo yes || echo no)
            run=$( { [ "$tok" = "yes" ] && [ "$dom" = "yes" ] && [ "$ids" = "yes" ]; } && echo yes || echo no)
            echo "| Zoho       | $tok   | api_domain=$dom    | $ids                     | $run       |"

            echo ""
            echo "Provider rotation runbook: \`docs/runbooks/crm-sandbox-credentials.md\`"
          } | tee -a "$GITHUB_STEP_SUMMARY" > .ci-out/providers.md

      - name: Run live-sandbox integration suite (cached-identity validators)
        id: vitest
        run: |
          mkdir -p .ci-out
          npx vitest run \
            platform/integrations/connectors/adapters/validateCachedIdentity.integration.test.ts \
            --reporter=default \
            --reporter=json \
            --outputFile=.ci-out/vitest.json

      # Sibling suite: exercises each adapter's `appointment.booked`
      # `execute()` against the same sandboxes (write path, with cleanup).
      # Runs as a separate vitest invocation so a failure in the read-side
      # parser drift suite doesn't mask drift in the auto-promote write
      # path (and vice versa). `if: always()` ensures the second suite
      # still runs even when the first one failed, so a single CI run
      # gives complete drift coverage instead of stopping at the first
      # red provider.
      - name: Run live-sandbox integration suite (appointment.booked auto-promote)
        id: vitest_appointment_booked
        if: always()
        run: |
          mkdir -p .ci-out
          npx vitest run \
            platform/integrations/connectors/adapters/appointmentBooked.integration.test.ts \
            --reporter=default \
            --reporter=json \
            --outputFile=.ci-out/vitest-appointment-booked.json

      # When zero blocks ran (no provider secrets configured yet), surface a
      # warning instead of failing — operations brings each provider online
      # one at a time per the runbook, and the workflow needs to land green
      # before the first sandbox is provisioned. Vitest itself fails the job
      # on real test failures.
      #
      # TODO(once all four providers are provisioned): tighten this to a hard
      # failure (exit 1 instead of `::warning::`), or assert
      # `passed >= EXPECTED_PROVIDER_COUNT`, so drift coverage cannot silently
      # regress back to zero if a secret store gets wiped.
      - name: Warn if zero provider blocks ran
        if: always() && (steps.vitest.outcome == 'success' || steps.vitest_appointment_booked.outcome == 'success')
        run: |
          summarise() {
            local label="$1" path="$2"
            if [ ! -f "$path" ]; then
              echo "::warning::$label: vitest output missing — could not verify how many blocks ran."
              return
            fi
            node -e '
              const [label, path] = process.argv.slice(1);
              const j = JSON.parse(require("fs").readFileSync(path,"utf8"));
              const passed = j.numPassedTests ?? 0;
              const failed = j.numFailedTests ?? 0;
              const pending = j.numPendingTests ?? 0;
              console.log(`vitest [${label}]: passed=${passed} failed=${failed} pending(skipped)=${pending}`);
              if (passed === 0 && failed === 0) {
                console.log(`::warning::Zero ${label} blocks ran — every provider is currently skipped. See docs/runbooks/crm-sandbox-credentials.md to wire the first provider.`);
              }
            ' "$label" "$path"
          }
          summarise "cached-identity validators"        .ci-out/vitest.json
          summarise "appointment.booked auto-promote"   .ci-out/vitest-appointment-booked.json

      # ─── Alert the on-call channel on failure ─────────────────────────────
      # Vitest itself fails the job on a real provider failure (upstream shape
      # drift, stale fixture, mint-step credential rotation needed, etc.).
      # A red checkmark on a daily cron is easy to miss, so we also open (or
      # update) a single deduplicated GitHub tracking issue labelled
      # `crm-cached-identity-drift`. The label is wired into the on-call
      # notification routing so the integrations on-call sees a real ping
      # instead of having to remember to inspect the daily Actions tab.
      #
      # When a subsequent run comes back green, the auto-close step closes
      # any open tracking issue from a previous drift run so the team isn't
      # left chasing already-resolved alerts.
      #
      # Mirrors the pattern used by `check-openai-realtime-voices.yml`.
      # Triggers on ANY job failure, not just vitest's — a failed Salesforce
      # / Zoho mint step is itself an actionable signal (long-lived inputs
      # stale) that the on-call needs to see, and if that step blows up
      # vitest never runs so `steps.vitest.outcome` would be 'skipped'.
      - name: Build tracking-issue body on failure
        if: failure()
        run: |
          mkdir -p .ci-out
          {
            echo "The daily live CRM sandbox suite failed."
            echo
            echo "Two suites run back-to-back against each provider's sandbox; either or both may have failed. Until this clears, the corresponding production behaviour for the affected provider is at risk:"
            echo
            echo "- **Cached-identity validators** (\`validateCachedIdentity.integration.test.ts\`) — drift here means \`CrmCallerIdentityRevalidationScheduler\` may stop scrubbing deleted records, so tenants will start seeing reactivated stale contact links in their pipelines."
            echo "- **\`appointment.booked\` auto-promote** (\`appointmentBooked.integration.test.ts\`) — drift here means a booked appointment may stop creating the Deal/Opportunity (or land it in the wrong pipeline/stage), so pipeline reports go quiet on the booking handoff."
            echo
            echo "Failures can also originate upstream of vitest — the per-run access-token mint step (Salesforce / Zoho) failing means the long-lived inputs are stale and need rotation."
            echo
            echo "**First responder checklist** (full runbook: \`docs/runbooks/crm-sandbox-credentials.md\`):"
            echo
            echo "1. Open the workflow run linked below and identify which step failed (mint, validator vitest, or booking vitest)."
            echo "2. **Mint step failed** (Salesforce / Zoho) → rotate the corresponding long-lived credential per the provider's section in the runbook and re-run."
            echo "3. **Vitest 401 / token-expired** (HubSpot / Pipedrive) → rotate the static sandbox token per the runbook and re-run."
            echo "4. **Validator vitest parser mismatch** (e.g. HubSpot 404 body shape moved, Zoho 200-wrapper changed, Salesforce key prefix changed) → patch the corresponding \`validate*CachedIdentity\` function in \`platform/integrations/connectors/adapters/\` and add a regression case to \`validateCachedIdentity.test.ts\`."
            echo "5. **Booking vitest stage/pipeline assertion failed** → the provider silently moved its default deal stage; either update \`*_SANDBOX_APPOINTMENT_PIPELINE_ID\` / \`_STAGE_ID\` to the new stage or patch the auto-promote contract in the corresponding adapter."
            echo "6. **Booking vitest cleanup warnings in log** → check the sandbox for leftover \`Apt Booked Co\` / \`+1555\` fixtures; the in-suite namespace sweep tries to mop these up but a partial network failure may leak rows."
            echo "7. **Validator vitest reports the deleted fixture as alive** → the fixture was reseeded or undeleted; re-delete it (and purge from the Recycle Bin for Salesforce) per the runbook."
            echo
            echo "## Vitest step outcomes"
            echo
            echo "- \`steps.vitest.outcome\` (cached-identity validators) = \`${{ steps.vitest.outcome }}\`"
            echo "- \`steps.vitest_appointment_booked.outcome\` (appointment.booked auto-promote) = \`${{ steps.vitest_appointment_booked.outcome }}\`"
            echo
            echo "(\`skipped\` means the failure was upstream of that vitest step, e.g. the mint step.)"
            echo
            echo "## Provider pre-flight"
            echo
            cat .ci-out/providers.md 2>/dev/null || echo "_(provider pre-flight summary unavailable)_"
            echo
            echo "## vitest summary — cached-identity validators"
            echo
            echo '```json'
            cat .ci-out/vitest.json 2>/dev/null || echo "(vitest.json missing — see workflow log for raw output; this is expected when the failure happened before vitest ran)"
            echo '```'
            echo
            echo "## vitest summary — appointment.booked auto-promote"
            echo
            echo '```json'
            cat .ci-out/vitest-appointment-booked.json 2>/dev/null || echo "(vitest-appointment-booked.json missing — see workflow log for raw output; this is expected when the failure happened before that step ran)"
            echo '```'
            echo
            echo "Workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          } > .ci-out/issue-body.md

      - name: Open or update tracking issue on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('.ci-out/issue-body.md', 'utf8');
            const title = 'CRM cached-identity validator suite is failing against live sandboxes';
            const label = 'crm-cached-identity-drift';

            // Best-effort: ensure our tracking label exists so the
            // de-duplication search is reliable on first run.
            try {
              await github.rest.issues.getLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: label,
              });
            } catch (err) {
              if (err.status === 404) {
                await github.rest.issues.createLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  name: label,
                  color: 'd73a4a',
                  description: 'Tracking issue for the daily live CRM sandbox validator suite. Routed to integrations on-call.',
                });
              } else {
                throw err;
              }
            }

            // De-duplicate: if there's already an open issue with our
            // label, update its body and add a comment so the on-call
            // channel pings again on consecutive failures.
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: label,
              per_page: 10,
            });

            if (existing.data.length > 0) {
              const issue = existing.data[0];
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body,
              });
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body:
                  `Re-detected on ${new Date().toISOString()} — see updated body above. ` +
                  `Workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              });
              core.notice(`Updated existing tracking issue #${issue.number}.`);
            } else {
              const created = await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: [label],
              });
              core.notice(`Opened tracking issue #${created.data.number}.`);
            }

      - name: Auto-close tracking issue when suite is back in sync
        if: success() && steps.vitest.outcome == 'success' && steps.vitest_appointment_booked.outcome == 'success'
        uses: actions/github-script@v7
        with:
          script: |
            // When both vitest steps succeed we close any open tracking
            // issue from a previous drift run so on-call isn't left
            // chasing an already-resolved alert. Comment first so the
            // audit trail links to the run that verified the fix.
            //
            // Guard: a "successful" vitest run with zero passed tests
            // across both suites means every provider block was skipped
            // (no sandbox secrets configured) — that is NOT evidence the
            // drift is resolved, so we must not auto-close in that case.
            // We sum passed tests from both JSON outputs so green-because-
            // -one-suite-ran-the-other-was-skipped still auto-closes.
            const fs = require('fs');
            const readPassed = (path) => {
              try {
                const j = JSON.parse(fs.readFileSync(path, 'utf8'));
                return j.numPassedTests ?? 0;
              } catch (err) {
                core.warning(`Could not read ${path}: ${err.message}.`);
                return null;
              }
            };
            const validatorPassed = readPassed('.ci-out/vitest.json');
            const bookingPassed = readPassed('.ci-out/vitest-appointment-booked.json');
            if (validatorPassed === null && bookingPassed === null) {
              core.warning('Could not read either vitest output to verify provider coverage. Skipping auto-close as a precaution.');
              return;
            }
            const totalPassed = (validatorPassed ?? 0) + (bookingPassed ?? 0);
            if (totalPassed === 0) {
              core.warning('Both vitest suites succeeded but zero provider blocks ran — refusing to auto-close any open `crm-cached-identity-drift` tracking issue, since "green because skipped" is not evidence the drift is resolved.');
              return;
            }
            const label = 'crm-cached-identity-drift';
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: label,
              per_page: 10,
            });
            for (const issue of existing.data) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body:
                  `Live CRM sandbox validator suite is back in sync as of ${new Date().toISOString()} — ` +
                  `auto-closing. Workflow run: ` +
                  `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              });
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                state: 'closed',
                state_reason: 'completed',
              });
              core.notice(`Auto-closed tracking issue #${issue.number}.`);
            }

      - name: Upload vitest output on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: crm-cached-identity-validators
          path: .ci-out/
          if-no-files-found: ignore
          retention-days: 14

